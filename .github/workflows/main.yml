on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: 	78.153.210.22
        username: clientftp
        password: ${{ secrets.ftp_password }}
        local-dir: webspace/httpdocs/client.westatlanticdesign.ie/
        exclude: | 
          **/.git*
          **/.scss
          **/.sass
          **/css.map
          **/.yml
          **/.md
